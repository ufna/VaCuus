#!/usr/bin/env bash
# Sits at EXACTLY a whitelisted package-relative path (Source/VaCuusJs/gen_relays.sh)
# so fab_scan's self-test 2 proves the whitelist ADMITS what it names: this shebang
# must NOT be reported in whitelist mode. Not the real generator — see
# Source/VaCuusJs/gen_relays.sh in the plugin tree for that.
