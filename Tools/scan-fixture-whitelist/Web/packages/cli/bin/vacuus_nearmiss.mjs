#!/usr/bin/env node
// The near-miss: same directory, same shebang, a name ONE suffix away from the
// whitelisted entry. fab_scan's self-test 2 proves the whitelist REFUSES what it does
// not name: this file must be reported as SHEBANG in whitelist mode.
